# TODO: Link Frontend to Backend

## 1. Update vite.config.js
- [x] Add server.proxy configuration to proxy '/api' requests to 'http://127.0.0.1:5555'

## 2. Update client.js
- [x] Change baseURL to '/api' for relative API calls

## Followup Steps
- [x] Run the backend server (cd econudge/backend && python app.py)
- [x] Run the frontend dev server (cd econudge/frontend && npm run dev)
- [ ] Test API calls (e.g., login/register) to ensure linking works
